stages:
  - build

build:
  stage: build
  image: golang:latest
  before_script:
  script: 
    # Compile and name the binary as `hello-go`
    - go build -o hello-go
    # Execute the binary
    - ./hello-go